# Print a top_items object

Print a top_items object

## Usage

``` r
# S3 method for class 'top_items'
print(x, ...)
```

## Arguments

- x:

  A `top_items` object (produced by
  [`top_items()`](https://jmgirard.github.io/ackwards/reference/top_items.md)).

- ...:

  Ignored.

## Value

`x` invisibly.
