# Print a suggest_k object

Print a suggest_k object

## Usage

``` r
# S3 method for class 'suggest_k'
print(x, ...)
```

## Arguments

- x:

  A `suggest_k` object.

- ...:

  Ignored.

## Value

`x` invisibly.
