# M87: Plain-English pass — vignettes B (girard, forbes, forbes2023, ordinal, interpret)

**Status:** done (2026-09-16, PR #95 https://github.com/jmgirard/ackwards/pull/95)

**Goal:** Rewrite the girard, forbes, forbes2023, ordinal, and interpret vignette sources into plain English that passes the M85 prose checker without changing any code, inline code, or rendered output, and widen the dod-gate prose step to the checker's full default domain.

**Outcome:** `vignettes/ackwards-girard.Rmd.orig`, `ackwards-forbes.Rmd.orig`, `ackwards-forbes2023.Rmd.orig`, `ackwards-ordinal.Rmd.orig`, and the live `ackwards-interpret.Rmd` rewritten: no dash, semicolon, banned phrase, or over-long sentence in prose; every `tools/prose-terms.txt` term glossed at its first prose use per vignette (bibliography titles count as not appearing). Every fenced chunk line and inline `r` span byte-identical to the merge base (the live interpret file compared by hand with `.code_lines_rmd()`, 79 lines). The four protected claims kept: the Forbes reproduction settings (`redundancy_criterion = "direct"`, `k_max = 10`, `n_obs = 3175`, 54/54 chase), edges as score correlations in a descriptive hierarchy, the Pearson default with the ordinal warning and `cor = "polychoric"` opt-in, and the `E4: Make friends easily` label. `tools/dod-gate.R`'s prose step calls `check_prose()` with no path argument (README, DESCRIPTION, NEWS, `R/`, all nine vignette sources). Four precomputed `.Rmd` regenerated with timing, check-mark glyph, and gt-id churn reverted to master; NEWS documentation entry, and the M85 entry's "vignettes are unchanged" sentence retired.

**Decisions:** none milestone-local.

**Review:** three fresh lenses. Nine findings, eight fixed at the gate: the forbes near-redundant paragraph had made `|r|` primary with `φ` an extra check where `prune()` flags a pair on either signal; the girard `suggest_k()` cell claimed every criterion uses the correlation matrix (CD resamples raw data); the girard redundancy gloss defined redundancy per factor, which also fit the retained node; the girard polychoric gloss correlated the items instead of their underlying continuous traits; two pronouns re-anchored to Forbes and to the retention rule; regeneration noise in the girard output (ten timing lines, seven `✔︎` lines) and a forbes2023 gt id reverted per LESSONS M61/M75, which also settled a NEWS finding about unchanged results. One rejected: the ESEM gloss "adds standard errors" is thin but not wrong and matches batch A. Blame-history lens: no findings. Merged on the full green CI matrix. Nothing graduated or retired.
