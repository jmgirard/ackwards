# M86: Plain-English pass — vignettes A (intro, suggest-k, engines, visualization)

**Status:** done (2026-09-16, PR #93 https://github.com/jmgirard/ackwards/pull/93)

**Goal:** Rewrite the intro, suggest-k, engines, and visualization vignette sources into plain English that passes the M85 prose checker without changing any code, inline code, or rendered output.

**Outcome:** `vignettes/ackwards-intro.Rmd.orig`, `ackwards-suggest-k.Rmd.orig`, `ackwards-engines.Rmd.orig`, and `ackwards-visualization.Rmd.orig` rewritten: no em or en dash, semicolon, banned phrase, or sentence over 30 words in prose; every `tools/prose-terms.txt` term glossed at its first prose use per vignette (a gloss paragraph precedes the engines comparison table); 27 argument and criterion headings changed from `—` to `:`; the dead engines anchor now points at `#fiml-for-continuous-pcaefa`; the redundancy gloss uses `prune()`'s direct-criterion definition; the varimax note says the package "does not offer" oblique rotation. Every fenced chunk line and inline `r` span is byte-identical to the merge base. `check_code_unchanged()` in `tools/check-prose.R` now reads every `vignettes/*.Rmd.orig` present on both sides of the merge base, with a planted-edit test (chunk option, chunk body, inline span) in `tests/testthat/test-check-prose.R`; `tools/dod-gate.R`'s prose step covers the four sources. The four precomputed `.Rmd` regenerated (only stamps, gt div ids, cli timing lines, and a check-mark variation selector differ in output); NEWS documentation entry.

**Decisions:** none milestone-local. Two gated criterion amendments at implement (AC4's engines claims rewritten to ones the vignette carries; AC2's instrument widened to the vignette sources).

**Review:** three fresh lenses; the diff-bug lens re-derived the code-unchanged claim independently. 13 findings: 4 fixed at the gate (the NEWS sentence had claimed an automated gate check the DoD gate never calls; a sentence split had given the WLSMV/ULSMV error the PCA/EFA route as antecedent; the `comparability()` gloss said two halves where the default is 10 repeated half-splits; a dropped hedge), 3 deferred to the prose-checker candidate row (span extraction is document-order-blind; a one-sided vignette source is skipped silently; table cells and headings are unchecked), 6 rejected (pre-existing varimax rationale vs D-034, anchor renames, table-cell full stops, glyph churn, cosmetics, a tracking read). Merged on local green under the CLAUDE.md CI override with seven checks pending. Nothing graduated or retired.
