# M88: Prose-checker hardening (`tools/check-prose.R`)

**Status:** done (2026-09-16, PR #94 https://github.com/jmgirard/ackwards/pull/94)

**Goal:** Fix the deferred M85 and M86 review findings on the prose checker and its code-unchanged guard before M87 runs.

**Outcome:** `tools/check-prose.R`: the word-list defaults resolve to the checker's own directory from any working directory (`.resolve_prose_script()` matches the loader frame by call name, `source`/`sys.source`/`base::` forms, then `--file=`, and makes the path absolute at load time; no hit stops instead of guessing `tools`). `.sentence_units()` counts each markdown heading and each table cell (split on unescaped pipes) as one standalone sentence under the 30-word cap, never split at internal terminators; separator rows carry none. `.blank_urls()` replaces link targets (one paren level, quoted title), autolinks, and bare `http(s)://` runs (ending at space, bracket, or brace) with same-length `x` runs before every report. `.code_lines_rmd()` returns `data.frame(line, text)` of chunk lines and inline `r` spans in document order, so a moved span is a change and a report names the working-tree line; a `vignettes/*.Rmd.orig` present on one side of the merge base is a problem in the R-loop form, and README.Rmd absent on both sides is none. `tools/dod-gate.R` runs `check_code_unchanged("master")` after the prose step when `DOD_CODE_UNCHANGED=1` (notes per problem, one failure naming the count) and prints a skip note otherwise. `tests/testthat/test-check-prose.R`: 115 assertions; 16 new ones fail on the pre-M88 checker and 6 more on the pre-review-fix head.

**Decisions:** none milestone-local. Plan-gate choices (opt-in gate step, one 30-word cap, blank URLs before all reports) are work-log lines.

**Review:** three fresh lenses; history and prior-review lenses reported nothing. The diff-bug lens returned 14 findings: 8 fixed at the gate with discriminating tests (README absent on both sides reported as one-sided; nested-paren and titled link targets leaking marks; bare URL running past `}`; escaped pipe splitting a cell; `base::sys.source` unrecognised; relative script path resolved at call time; any `ofile` frame accepted; a heading with an internal period split against AC2's one-sentence rule), 6 rejected (`--file=` fallback is the CLI route; one-sided message form fixed by AC6; hard-coded `master` and error-as-failure match AC4; non-http schemes outside AC3; cosmetic `after line 0`; vector growth). Merged on local green under the CLAUDE.md CI override with seven checks pending. One LESSONS line added; nothing graduated or retired.
