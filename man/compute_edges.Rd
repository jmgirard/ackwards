% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_edges.R
\name{compute_edges}
\alias{compute_edges}
\title{Compute between-level factor-score correlations}
\usage{
compute_edges(
  levels,
  R,
  edge_method = c("auto", "algebra", "scores"),
  pairs = c("adjacent", "all"),
  data = NULL,
  align = TRUE,
  use = "pairwise.complete.obs",
  cut_show = 0.3
)
}
\arguments{
\item{levels}{Named list (indexed by k) of per-level objects produced by an
engine. Each must contain a \code{scoring} sub-list with fields \code{linear},
\code{weights}, and \code{score_var}.}

\item{R}{Square correlation matrix (p × p). Required for the algebra path.}

\item{edge_method}{One of \code{"auto"} (algebra when possible, scores otherwise),
\code{"algebra"} (force; errors if conditions not met), or \code{"scores"} (always
materialise).}

\item{pairs}{\code{"adjacent"} (classic Goldberg) or \code{"all"} (Forbes extension).}

\item{data}{Optional data frame / matrix of raw observations. Required only
when \code{edge_method = "scores"} or the scores path is triggered.}

\item{align}{Whether to sign-align edges to primary-parent lineage.}

\item{use}{Passed to \code{\link[stats:cor]{stats::cor()}} when materialising scores.}

\item{cut_show}{Edges with \verb{|r| >= cut_show} are flagged \code{above_cut} in the
tidy tibble.}
}
\value{
A list with:
\item{matrices}{Named list of \verb{(k_a × k_b)} edge matrices, keyed
\code{"k_a:k_b"}.}
\item{tidy}{A data frame with one row per directed edge: \code{from}, \code{to},
\code{level_from}, \code{level_to}, \code{r}, \code{is_primary}, \code{above_cut}.}
}
\description{
The centerpiece of the bass-ackwards algebra. For any engine whose scoring
is a \strong{linear} map \verb{S = Z W}, the cross-level correlation matrix is:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{E(a,b) = D_a^\{-1/2\} (W_a' R W_b) D_b^\{-1/2\}
}\if{html}{\out{</div>}}

where \code{R} is the input correlation matrix and \verb{D_x = diag(W_x' R W_x)} are
the \strong{actual} score variances (not assumed to be 1). This avoids
materialising scores while remaining exact for PCA, EFA (regression /
Bartlett / tenBerge) — all of which produce linear score maps.

When the algebra cannot be used (nonlinear scoring, missing \code{R}, or the user
forces \code{edge_method = "scores"}), scores are materialised from \code{data} instead.
}
\keyword{internal}
