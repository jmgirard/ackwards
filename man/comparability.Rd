% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparability.R
\name{comparability}
\alias{comparability}
\title{Split-half factor comparability}
\usage{
comparability(
  data,
  k_max,
  engine = "pca",
  cor = "pearson",
  fm = "minres",
  n_splits = 10L,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame or numeric matrix of observed variables (items in
columns, observations in rows). \strong{Raw data only} -- splitting needs rows,
so a correlation matrix is not accepted. Missing values are handled
pairwise throughout (as in \code{\link[=ackwards]{ackwards()}}'s default).}

\item{k_max}{Maximum number of factors/components to evaluate -- normally
the same value (or one or two above it) you intend to pass to
\code{\link[=ackwards]{ackwards()}}. Required.}

\item{engine}{Extraction engine: \code{"pca"} (default) or \code{"efa"}. \code{"esem"} is
not yet supported here (fitting \code{2 * n_splits} lavaan hierarchies needs
its own performance treatment); for ESEM workflows, run
\code{comparability()} with \code{engine = "efa"} as a structural screen.}

\item{cor}{Correlation basis: \code{"pearson"} (default) or \code{"spearman"}. As
with \code{\link[=suggest_k]{suggest_k()}}, \code{"polychoric"} is not supported (estimating
polychoric matrices in every half-sample is slow and unstable); users
analysing ordinal data should screen replicability on the Pearson basis
and fit the final model with \code{cor = "polychoric"} in \code{\link[=ackwards]{ackwards()}}.}

\item{fm}{Factor extraction method passed to \code{\link[psych:fa]{psych::fa()}}; only used when
\code{engine = "efa"}. One of \code{"minres"} (default), \code{"ml"}, or \code{"pa"}.}

\item{n_splits}{Number of random split-half replicates. Default \code{10L}
(repeated random splits, following Goldberg's practice -- a single split
can mislead by luck of the draw). Each replicate fits \code{2 * k_max}
solutions, so the default costs 20 hierarchy fits; PCA and EFA are fast
enough that this is typically a few seconds.}

\item{seed}{Integer seed for reproducible splits. \code{NULL} (default) uses the
current RNG state.}

\item{...}{Reserved for future arguments.}
}
\value{
An object of class \code{"comparability"}. Print it for a per-level
summary; call \code{\link[=autoplot]{autoplot()}} on it for a diagnostic plot. The list contains:
\item{coefficients}{Data frame with one row per split x level x factor:
\code{split}, \code{level}, \code{factor} (full-sample \verb{m\{k\}f\{j\}} label), \code{r} (score
comparability), \code{phi} (Tucker's congruence of the matched loading
columns). \code{NA} when the level did not converge in one of the halves.}
\item{summary}{Data frame with one row per level x factor: \code{level},
\code{factor}, \code{r_median}, \code{r_min}, \code{phi_median}, \code{phi_min} (across splits),
and \code{n_splits_ok} (splits in which both halves converged).}
\item{k_max}{Deepest level evaluated. Can be lower than the \code{k_max} you
asked for when the full-sample fit truncated (non-convergence at deep
levels); the original request is kept in \code{k_requested}.}
\item{k_requested, n_splits, n_half, engine, cor, fm, n_obs, n_vars,
seed}{Metadata.}
}
\description{
Measures how well each factor at each level of a bass-ackwards hierarchy
\strong{replicates} across random split-halves of the sample -- Everett's (1983)
factor comparability coefficients, the depth gate Goldberg's own lab applied
to its hierarchies (Goldberg, 1990) and the direct instrument for the
overextraction caution in \code{\link[=suggest_k]{suggest_k()}}: non-replicable structure
concentrates in the deeper levels of an overextracted hierarchy
(Forbes, 2023).
}
\details{
For each of \code{n_splits} random half-splits, solutions at every level
\verb{1..k_max} are fit independently in each half. Each half-solution's factors
are matched to the \strong{full-sample} solution's factors (so coefficients are
reported under the same \verb{m\{k\}f\{j\}} labels you get from \code{\link[=ackwards]{ackwards()}}), and
the comparability coefficient for a factor is the correlation between its
two matched half-solution scores, computed on the pooled correlation matrix
via the same \verb{W'RW} algebra used for between-level edges -- applying both
halves' scoring weights to the full sample, exactly Everett's procedure.
Tucker's congruence coefficient (phi) between the matched half-solution
loading columns is reported alongside: comparability asks whether the two
halves' \emph{scores} agree; phi asks whether their \emph{loading patterns} agree.
}
\section{Interpreting the output}{

Coefficients near 1 mean the factor re-emerges in independent half-samples;
a factor whose comparability is low is sample-idiosyncratic and should not
anchor substantive interpretation. Goldberg's lab treated roughly .95 as
comfortable and .90 as a floor; these are conventions, not tests, so
\code{comparability()} reports every coefficient and flags nothing. The deepest
level at which all factors replicate is a natural \strong{hierarchy floor} for
\code{\link[=ackwards]{ackwards()}}'s \code{k_max}; see \code{vignette("ackwards-girard")} for the full
workflow.

A level that fails to converge in a half-sample yields \code{NA} coefficients
for that split (convergence is data, not an error); the number of usable
splits per factor is reported in \code{summary$n_splits_ok} and a message
summarises any shortfall.
}

\examples{
\donttest{
cmp <- comparability(bfi25, k_max = 5, n_splits = 5, seed = 1)
cmp
cmp$summary
}

}
\references{
Everett, J. E. (1983). Factor comparability as a means of determining the
number of factors and their rotation. \emph{Multivariate Behavioral Research},
18(2), 197--218. \doi{10.1207/s15327906mbr1802_5}

Goldberg, L. R. (1990). An alternative "description of personality": The
Big-Five factor structure. \emph{Journal of Personality and Social
Psychology}, 59(6), 1216--1229. \doi{10.1037/0022-3514.59.6.1216}

Forbes, M. K. (2023). Improving hierarchical models of individual
differences: An extension of Goldberg's bass-ackward method.
\emph{Psychological Methods}. \doi{10.1037/met0000546}
}
\seealso{
\code{\link[=suggest_k]{suggest_k()}} for the plausible depth \emph{range} (eigenstructure),
\code{\link[=prune]{prune()}} for factors that perpetuate without differentiating
(redundancy), and \code{\link[=ackwards]{ackwards()}} for the extraction itself.
}
