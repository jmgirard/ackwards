% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparability.R
\name{comparability}
\alias{comparability}
\title{Split-half factor comparability}
\usage{
comparability(
  data,
  k_max,
  engine = "pca",
  cor = "pearson",
  fm = "minres",
  n_splits = 10L,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame or numeric matrix of observed variables (items in
columns, observations in rows). \strong{Raw data only}, because splitting needs
rows, so a correlation matrix is not accepted. Missing values are handled
pairwise throughout (as in \code{\link[=ackwards]{ackwards()}}'s default).}

\item{k_max}{Maximum number of factors or components to evaluate. Normally
this is the same value (or one or two above it) you intend to pass to
\code{\link[=ackwards]{ackwards()}}. Required.}

\item{engine}{Extraction engine: \code{"pca"} (default) or \code{"efa"}. \code{"esem"} is
not yet supported here, because fitting \code{2 * n_splits} lavaan hierarchies
needs its own performance treatment. For ESEM (exploratory structural
equation modeling) workflows, run \code{comparability()} with
\code{engine = "efa"} as a structural screen.}

\item{cor}{Correlation basis: \code{"pearson"} (default) or \code{"spearman"}. As
with \code{\link[=suggest_k]{suggest_k()}}, \code{"polychoric"} is not supported, because estimating
polychoric matrices in every half-sample is slow and unstable. Users
analysing ordinal items (a few ordered categories, such as a 1 to 5
rating) should screen replicability on the Pearson basis. They should
then fit the final model with \code{cor = "polychoric"} in \code{\link[=ackwards]{ackwards()}}, which
estimates the correlation between the continuous traits assumed to
underlie the ordered responses.}

\item{fm}{Factor extraction method passed to \code{\link[psych:fa]{psych::fa()}}. It is only used
when \code{engine = "efa"}. One of \code{"minres"} (default), \code{"ml"}, or \code{"pa"}.}

\item{n_splits}{Number of random split-half replicates. Default \code{10L}.
The published precedents used a single split (Saucier et al., 2005).
Repeating the split guards against the luck of one draw, so the
coefficients are summarised over replicates. Each replicate fits \code{2 * k_max}
solutions, so the default costs 20 hierarchy fits. PCA (principal
component analysis) and EFA (exploratory factor analysis) are fast
enough that this is typically a few seconds.}

\item{seed}{Integer seed for reproducible splits. \code{NULL} (default) uses the
current RNG state.}

\item{...}{Reserved for future arguments.}
}
\value{
An object of class \code{"comparability"}. Print it for a per-level
summary, or call \code{\link[=autoplot]{autoplot()}} on it for a diagnostic plot. The list
contains:
\item{coefficients}{Data frame with one row per split x level x factor.
Its columns are \code{split}, \code{level}, \code{factor} (full-sample \verb{m\{k\}f\{j\}}
label), \code{r} (score comparability), and \code{phi} (Tucker's congruence of
the matched loading columns). A value is \code{NA} when the level did not
converge in one of the halves.}
\item{summary}{Data frame with one row per level x factor. Its columns
are \code{level}, \code{factor}, \code{r_median}, \code{r_min}, \code{phi_median}, \code{phi_min}
(across splits), and \code{n_splits_ok} (splits in which both halves
converged).}
\item{k_max}{Deepest level evaluated. It can be lower than the \code{k_max}
you asked for when the full-sample fit truncated, that is, when deep
levels did not converge. The original request is kept in
\code{k_requested}.}
\item{k_requested, n_splits, n_half, engine, cor, fm, n_obs, n_vars,
seed}{Metadata.}
}
\description{
Measures how well each factor at each level of a bass-ackwards hierarchy
\strong{replicates} across random split-halves of the sample. A factor is a
summary variable standing in for a group of items that move together. In a
split-half analysis the sample is split into two random halves and each
half is analysed separately. The coefficients are Everett's (1983) factor
comparability coefficients. They revive the split-half replication gate of
the research program that produced the bass-ackwards method. Saucier (1997)
screened factor solutions by their split-half stability, and Saucier,
Georgiades, Tsaousis, and Goldberg (2005) chose the optimal hierarchical
level by requiring split-half replication above a .90 threshold. This is
also the direct instrument for the overextraction caution in \code{\link[=suggest_k]{suggest_k()}},
because non-replicable structure concentrates in the deeper levels of an
overextracted hierarchy (Forbes, 2023).
}
\details{
For each of \code{n_splits} random half-splits, solutions at every level from 1
to \code{k_max} are fit independently in each half. Each half-solution's factors
are matched to the \strong{full-sample} solution's factors, so coefficients are
reported under the same \verb{m\{k\}f\{j\}} labels you get from \code{\link[=ackwards]{ackwards()}}. The
comparability coefficient for a factor is the correlation between its two
matched half-solution scores. It is computed on the pooled correlation
matrix via the same \verb{W'RW} algebra used for between-level edges, applying
both halves' scoring weights to the full sample, exactly Everett's
procedure. Tucker's congruence coefficient (phi) between the matched
half-solution loading columns is reported alongside. A loading is the
correlation between an item and a factor, and congruence is a 0 to 1 index
of how similar two loading patterns are. So comparability asks whether the
two halves' \emph{scores} agree, and phi asks whether their \emph{loading patterns}
agree.
}
\section{Interpreting the output}{

Coefficients near 1 mean the factor re-emerges in independent half-samples.
A factor whose comparability is low is sample-idiosyncratic and should not
anchor substantive interpretation. Two benchmarks are conventional. The
first, \strong{.90}, is the split-half replication threshold of Goldberg's
lexical research program (Saucier et al., 2005). It follows from Everett's
(1983) rationale, under which split-half factors share at least 81\% of
their variance. The second, \strong{.95}, is the stricter bound at which two
factors are conventionally treated as interchangeable (Lorenzo-Seva & ten
Berge, 2006). These are conventions, not tests, so \code{comparability()}
reports every coefficient and flags nothing. The deepest level at which all
factors replicate is a natural \strong{hierarchy floor} for \code{\link[=ackwards]{ackwards()}}'s
\code{k_max}. See \code{vignette("ackwards-girard")} for the full workflow.

A level that fails to converge in a half-sample yields \code{NA} coefficients
for that split, because convergence is data, not an error. The number of
usable splits per factor is reported in \code{summary$n_splits_ok} and a message
summarises any shortfall.
}

\examples{
\donttest{
cmp <- comparability(sim16, k_max = 5, n_splits = 5, seed = 1)
cmp
cmp$summary
}

}
\references{
Everett, J. E. (1983). Factor comparability as a means of determining the
number of factors and their rotation. \emph{Multivariate Behavioral Research},
18(2), 197--218. \doi{10.1207/s15327906mbr1802_5}

Saucier, G. (1997). Effects of variable selection on the factor structure
of person descriptors. \emph{Journal of Personality and Social Psychology},
73(6), 1296--1312. \doi{10.1037/0022-3514.73.6.1296}

Saucier, G., Georgiades, S., Tsaousis, I., & Goldberg, L. R. (2005). The
factor structure of Greek personality adjectives. \emph{Journal of
Personality and Social Psychology}, 88(5), 856--875.
\doi{10.1037/0022-3514.88.5.856}

Lorenzo-Seva, U., & ten Berge, J. M. F. (2006). Tucker's congruence
coefficient as a meaningful index of factor similarity.
\emph{Methodology}, 2(2), 57--64. \doi{10.1027/1614-2241.2.2.57}

Forbes, M. K. (2023). Improving hierarchical models of individual
differences: An extension of Goldberg's bass-ackward method.
\emph{Psychological Methods}. \doi{10.1037/met0000546}
}
\seealso{
\code{\link[=suggest_k]{suggest_k()}} for the plausible depth \emph{range} (eigenstructure),
\code{\link[=factorability]{factorability()}} for sampling adequacy before you fit, \code{\link[=prune]{prune()}} for
factors that perpetuate without differentiating (redundancy), and
\code{\link[=ackwards]{ackwards()}} for the extraction itself.
}
