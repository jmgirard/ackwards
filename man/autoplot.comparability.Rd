% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparability.R
\name{autoplot.comparability}
\alias{autoplot.comparability}
\title{Plot a comparability diagnostic}
\usage{
\method{autoplot}{comparability}(object, ...)
}
\arguments{
\item{object}{A \code{comparability} object.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object.
}
\description{
Renders a two-panel ggplot2 diagnostic for a \code{\link[=comparability]{comparability()}} object:
score comparability (r) and loading congruence (Tucker's phi) for every
factor at every level. Grey points are individual splits; black points are
the per-factor medians. Dashed and dotted reference lines mark the
conventional .90 / .95 benchmarks -- visual guides, not tests.
}
\details{
Requires the \pkg{ggplot2} package.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  cmp <- comparability(sim16, k_max = 5, n_splits = 5, seed = 1)
  autoplot(cmp)
}
}

}
\seealso{
\code{\link[=comparability]{comparability()}}
}
