% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{bfi25}
\alias{bfi25}
\title{Big Five Inventory -- 25-item IPIP example dataset}
\format{
A data frame with 1 000 rows and 25 integer columns (Likert responses
scored 1--6, with some \code{NA}s reflecting genuine missing values in the
original survey). The 25 items span the Big Five personality domains:\tabular{ll}{
   Columns \tab Domain \cr
   \code{A1}--\code{A5} \tab Agreeableness \cr
   \code{C1}--\code{C5} \tab Conscientiousness \cr
   \code{E1}--\code{E5} \tab Extraversion \cr
   \code{N1}--\code{N5} \tab Neuroticism \cr
   \code{O1}--\code{O5} \tab Openness \cr
}
}
\source{
Revelle, W. (2026). \emph{psych: Procedures for Psychological, Psychometric, and
Personality Research}. Northwestern University, Evanston, Illinois.
R package. \url{https://CRAN.R-project.org/package=psych}

Data collected via the SAPA (Synthetic Aperture Personality Assessment)
project (\url{https://www.sapa-project.org/}). Items are public-domain IPIP
(International Personality Item Pool) markers developed by Goldberg (1999):

Goldberg, L. R. (1999). A broad-bandwidth, public domain, personality
inventory measuring the lower-level facets of several five-factor models.
In I. Mervielde, I. Deary, F. De Fruyt, & F. Ostendorf (Eds.),
\emph{Personality Psychology in Europe}, Vol. 7, pp. 7--28. Tilburg University Press.
}
\usage{
bfi25
}
\description{
A 1 000-row subset of the 25 IPIP Big Five personality-marker items from
Revelle's \code{psych} package (\code{psych::bfi}). Included so that package examples
and vignettes run without reaching into \code{psych}'s namespace directly.
}
\details{
Derived from \code{psych::bfi[, 1:25]} by sampling 1 000 rows with
\code{set.seed(42)}. Missing values are preserved as in the original data.
The full dataset (\code{psych::bfi}) contains responses from 2 800 participants
collected via the SAPA project.

Each item column carries its public-domain IPIP stem (Goldberg, 1999) as a
\code{label} attribute, so \code{ackwards()} captures it at fit time and \code{top_items()}
prints the wording as \code{code: label} (e.g. \verb{E4: Make friends easily}) with
no setup. These are plain attributes: base row-subsetting (e.g.
\code{na.omit(bfi25)}, \code{bfi25[rows, ]}) drops them, as base R does for any
non-\code{labelled}-class vector, so fit on \code{bfi25} \strong{directly} -- its \code{NA}s are
handled by the \code{missing} argument of \code{ackwards()} -- to keep the labels.

To regenerate this dataset, run \code{source("data-raw/bfi25.R")} from the
package root.
}
\examples{
dim(bfi25)
head(bfi25)
attr(bfi25$E4, "label") # public-domain IPIP item stem
}
\keyword{datasets}
