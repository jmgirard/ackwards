% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret.R
\name{top_items}
\alias{top_items}
\title{Display the salient items for each factor}
\usage{
top_items(x, level = NULL, cut = 0.3, n = NULL, sort = TRUE)
}
\arguments{
\item{x}{An \code{ackwards} object.}

\item{level}{Integer vector selecting which level(s) to include. \code{NULL}
(default) returns all levels.}

\item{cut}{Absolute-loading threshold. Items with \verb{|loading| >= cut} are
shown. Default \code{0.3}, matching the \code{cut_show} plotting default.}

\item{n}{Maximum number of items to show per factor. \code{NULL} (default) shows
all items meeting the cut. When set, the top-\code{n} items by \verb{|loading|} are
kept after applying the cut.}

\item{sort}{Logical. When \code{TRUE} (default), items within each factor are
ordered by descending \verb{|loading|}. Set to \code{FALSE} to keep the original
item order (useful when items have a meaningful sequence).}
}
\value{
An object of class \code{"top_items"}. Print it for a grouped
level-by-factor cli listing. The underlying data frame (one row per
selected item) is accessible via \verb{$data} and contains columns \code{level},
\code{factor}, \code{item}, and \code{loading}. The values equal the corresponding
\code{tidy(x, what = "loadings")} rows (after filtering and optional sorting).
}
\description{
Returns, per level and factor, the items whose absolute loading meets or
exceeds \code{cut}, sorted by descending absolute loading. This gives a concise
reading of "what each factor is about" without printing a full item-by-factor
matrix, which does not scale well to large \code{k} or many items.
}
\details{
Loadings are signed and reflect the object's primary-parent sign alignment
(see \code{\link[=ackwards]{ackwards()}}). Items near the cut threshold may appear for one sign
orientation but not the other; this is expected and informative.
}
\examples{
x <- ackwards(bfi25, k_max = 5)
top_items(x)
top_items(x, level = 5, cut = 0.4, n = 5)

}
\seealso{
\code{\link[=tidy.ackwards]{tidy.ackwards()}}, \code{\link[=label_template]{label_template()}}, \code{\link[=autoplot.ackwards]{autoplot.ackwards()}}
}
