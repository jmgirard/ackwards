% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret.R
\name{top_items}
\alias{top_items}
\title{Display the salient items for each factor}
\usage{
top_items(
  x,
  level = NULL,
  cut = 0.3,
  n = NULL,
  sort = TRUE,
  by = c("factor", "item"),
  show_labels = TRUE
)
}
\arguments{
\item{x}{An \code{ackwards} object.}

\item{level}{Integer vector selecting which level(s) to include. \code{NULL}
(default) returns all levels.}

\item{cut}{Absolute-loading threshold. Items with \verb{|loading| >= cut} are
shown. Default \code{0.3}, matching the \code{cut_show} plotting default.}

\item{n}{Maximum number of items to show per factor. \code{NULL} (default) shows
all items meeting the cut. When set, the top-\code{n} items by \verb{|loading|} are
kept after applying the cut.}

\item{sort}{Logical. When \code{TRUE} (default), items within each group are
ordered by descending \verb{|loading|}. Set to \code{FALSE} to keep the original
order (useful when items have a meaningful sequence).}

\item{by}{One of \code{"factor"} (default) or \code{"item"}. \code{"factor"} groups the
listing by factor (the salient items \emph{of} each factor -- "what is this
factor about?"). \code{"item"} inverts the grouping to list, for each item, the
factors it loads on -- which makes cross-loadings legible ("where does this
item go?"). \code{n} and \code{sort} apply within whichever unit \code{by} selects.}

\item{show_labels}{Logical. When \code{TRUE} (default) and the data carried a
variable-label attribute at fit time (see \code{\link[=ackwards]{ackwards()}}), items are shown as
\code{id: label}; items without a label fall back to the bare id. Set to
\code{FALSE} to always show the bare \verb{m\{k\}f\{j\}}-style item ids.}
}
\value{
An object of class \code{"top_items"}. Print it for a grouped cli listing.
The underlying data frame (one row per selected item) is accessible via
\verb{$data} and contains columns \code{level}, \code{factor}, \code{item}, and \code{loading} (plus
\code{label} when labels are available). The values equal the corresponding
\code{tidy(x, what = "loadings")} rows (after filtering and optional sorting).
}
\description{
Returns, per level and factor, the items whose absolute loading meets or
exceeds \code{cut}, sorted by descending absolute loading. This gives a concise
reading of "what each factor is about" without printing a full item-by-factor
matrix, which does not scale well to large \code{k} or many items.
}
\details{
Loadings are signed and reflect the object's primary-parent sign alignment
(see \code{\link[=ackwards]{ackwards()}}). Items near the cut threshold may appear for one sign
orientation but not the other; this is expected and informative.

If \link[=set_factor_labels]{factor labels} have been attached, the factor
dimension is shown as \code{label (id)} wherever it appears -- the group headers
under \code{by = "factor"} and the body entries under \code{by = "item"}.
}
\examples{
# Fit the raw dataset (not na.omit(), which would drop the column
# attributes): bfi25's IPIP item labels are then captured and printed as
# `code: label`. `missing = "listwise"` handles the NAs cleanly. A 10-item
# subset keeps the example fast; use all items in practice.
x <- ackwards(bfi25[, 1:10], k_max = 3, cor = "polychoric", missing = "listwise")
top_items(x)
top_items(x, level = 3, cut = 0.4, n = 5)

# Invert the grouping to read cross-loadings item-by-item
top_items(x, level = 3, cut = 0.25, by = "item")

}
\seealso{
\code{\link[=tidy.ackwards]{tidy.ackwards()}}, \code{\link[=label_template]{label_template()}}, \code{\link[=set_factor_labels]{set_factor_labels()}},
\code{\link[=autoplot.ackwards]{autoplot.ackwards()}}
}
